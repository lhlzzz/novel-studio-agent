# Postiz Adapter Compatibility Path

The canonical Postiz provider is implemented in
`integrations/providers/postiz/`. This legacy import path remains only for
backward compatibility and must not contain a second HTTP implementation.
